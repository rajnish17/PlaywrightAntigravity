
let score1 = 75;
let result1 = score1 >= 80 ? "pass" : "fail"
console.log(result1)

let score2 = 85;
let result2 = score2 >= 85? "pass" : "fail"
console.log(result2)

function getResult(score2)
{
    return score2 >=65 ? "pass" : "fail"
}
getResult(56)
getResult(95)
